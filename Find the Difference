class Solution:
    def findTheDifference(self, s: str, t: str) -> str:
        a=''
        for i in t:
            if t.count(i)!=s.count(i) and i not in a:
                a+=i
        return a
    
       
