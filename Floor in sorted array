class Solution:
    def findFloor(self, arr, x):
        n = len(arr)
        i,j=0,n-1
        while i<=j:
            mid = (i+j)//2
            if arr[mid]<=x:
                i = mid+1
            else:
                j=mid-1
        return i-1
        
        
