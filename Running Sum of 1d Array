class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        total=0
        new_list=[]
        for i in range(len(nums)):
            total+=nums[i]
            new_list.append(total)
        return new_list
