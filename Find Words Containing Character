class Solution:
    def findWordsContaining(self, words: List[str], x: str) -> List[int]:
        lis=[]
        for word in range(len(words)):
            if x in words[word]:
                lis.append(word)
        return lis
        
